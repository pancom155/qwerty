<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Kitchen Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary-color: #ff6600;
      --gradient-bg: #343434;
    }

    body {
      background-color: var(--light-bg);
      font-family: 'Poppins', sans-serif;
      color: var(--dark-text);
    } 

    /* Navbar */
    .navbar-custom {
      border-radius: 40px;
      margin: 20px;
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    }

    .navbar-custom img {
      width: 45px;
      height: 45px;
      object-fit: cover;
      border-radius: 50%;
      border: 2px solid #988f8f;
      background-color: #fff;
    }

    .admin-info {
      margin-left: 10px;
      color: rgb(110, 110, 110);
    }

    .admin-name {
      font-weight: 600;
      margin-bottom: 0;
    }

    .admin-email {
      font-size: 0.85rem;
      color: #ababab;
      margin-bottom: 0; 
    }

    /* Heading */
    h2 {
      text-align: center;
      margin: 80px 0 30px 0;
      color: #343a40;
      font-size: 2.5rem;
    }

    h2 span {
      background-image: linear-gradient(to right, #ff6600, #ff7700);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      font-weight: bold;
    }

    /* Cards */
    .card {
      border: none;
      border-radius: 12px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      transition: all 0.3s ease;
      overflow: hidden;
      margin-bottom: 25px;
      border-left: 5px solid var(--primary-color);
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 71, 255, 0.1);
    }

    .card-header {
      background-color: white;
      border-bottom: 1px solid rgba(0, 0, 0, 0.05);
      padding: 15px 20px;
      font-weight: 600;
      display: flex;
      align-items: center;
    }

    .card-body {
      padding: 0;
    
    }

    .list-group-item {
      border: none;
      border-bottom: 1px solid rgba(0, 0, 0, 0.05);
      padding: 12px 15px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      word-break: break-word;
    }

    .list-group-item:last-child {
      border-bottom: none;
    }

    .badge.bg-primary {
      background: var(--gradient-bg) !important;
      font-weight: 500;
      font-size: 0.85rem;
      padding: 5px 10px;
      color: white;
    }

    .card-footer {
      background-color: white;
      border-top: 1px solid rgba(0, 0, 0, 0.05);
      padding: 15px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
    }

    .badge.bg-warning {
      background: var(--gradient-bg) !important;
      color: white !important;
      font-weight: 600;
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 0.85rem;
    }

    .time-badge {
      background-color: #f1f2f6;
      color: var(--light-text);
      padding: 5px 10px;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 500;
    }

    .order-number {
      display: inline-block;
      background: var(--gradient-bg);
      color: white;
      width: 30px;
      height: 30px;
      border-radius: 50%;
      text-align: center;
      line-height: 30px;
      font-weight: 600;
      margin-right: 10px;
    }

    /* Empty state */
    .empty-state {
      text-align: center;
      margin-top: 50px;
    }

    .empty-state i {
      font-size: 3rem;
      background: var(--gradient-bg);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 20px;
    }

    .empty-state h3 {
      color: var(--dark-text);
      margin-bottom: 10px;
    }

    .empty-state p {
      color: var(--light-text);
      font-size: 1.1rem;
    }

    /* Buttons */
    .btn-danger {
      margin-right: 20px;
      background-color: #ff6b6b;
      border: none;
    }

    .btn-danger:hover {
      background-color: #ff5252;
    }

    /* Responsive adjustments */
    @media (max-width: 1200px) {
      .card-header, .card-footer {
        font-size: 0.95rem;
        padding: 10px 15px;
      }

      .order-number {
        width: 28px;
        height: 28px;
        line-height: 28px;
        font-size: 0.85rem;
      }

      .badge.bg-primary {
        padding: 4px 8px;
        font-size: 0.75rem;
      }

      .time-badge {
        font-size: 0.75rem;
        padding: 4px 8px;
      }
    }

    @media (max-width: 992px) {
      .card-footer > div.text-end {
        width: 100%;
        text-align: left !important;
        margin-top: 10px;
      }
    }

    @media (max-width: 576px) {
      h2 {
        font-size: 1.8rem;
        margin: 60px 0 20px 0;
      }

      .card-body ul li {
        font-size: 0.85rem;
        padding: 8px 10px;
      }

      .empty-state i {
        font-size: 2.5rem;
      }

      .btn-danger {
        font-size: 0.8rem;
        padding: 5px 10px;
      }

      .admin-info {
        display: none; /* hide admin info on very small screens */
      }
    }
  </style>
</head>
<body>

<nav class="navbar navbar-custom navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center" href="#">
      <img src="/images/napslogo.png" alt="Restaurant Logo" style="height: 60px; width: 60px;">
    </a>
    
    <div class="d-flex align-items-center ms-auto">
      <div class="d-flex align-items-center me-4">
        <img src="/images/admin-avatar.avif" alt="Admin Avatar">
        <div class="admin-info d-none d-md-block">
          <p class="admin-name"><%= user.fullName %></p>
          <p class="admin-email"><%= user.email %></p>
        </div>
      </div>
      <a href="/logout" class="btn btn-sm btn-danger d-flex align-items-center">
        <i class="bi bi-box-arrow-right me-1"></i> Logout
      </a>
    </div>
  </div>
</nav>

<div class="container-fluid mt-4 px-4">
  <h2>Kitchen <span>Orders</span></h2>

  <div class="row">
    <% if (orders.length === 0) { %>
      <div class="col-12">
        <div class="empty-state">
          <i class="bi bi-emoji-frown"></i>
          <h3>No Active Orders</h3>
          <p>There are currently no orders in the kitchen queue</p>
        </div>
      </div>
    <% } else { %>
      <% orders.slice().reverse().forEach((order, index) => { %>
        <div class="col-xl-4 col-lg-6 col-md-6 col-sm-12 mb-4">
          <div class="card h-100">
            <div class="card-header d-flex justify-content-between align-items-center">
              <span class="order-number">#<%= index + 1 %></span>
              <% if (order.tableNumber) { %>
                <span><i class="bi bi-grid-1x2-fill me-1"></i>Table: <%= order.tableNumber %></span>
              <% } else { %>
                <span><i class="bi bi-house-door-fill me-1"></i>Online Order</span>
              <% } %>
            </div>

            <div class="card-body p-0">
              <ul class="list-group list-group-flush">
                <% order.items.forEach(item => { %>
                  <li class="list-group-item py-2 px-3">
                    <div class="d-flex justify-content-between align-items-center w-100">
                      <span><%= item.productId.name %> <strong>x <%= item.quantity %></strong></span>
                    </div>
                  </li>
                <% }) %>
              </ul>
            </div>

            <div class="card-footer">
              <div>
                <div class="mb-2">
                  <i class="bi bi-person-fill me-2"></i>
                  <strong>
                    <% if (order.userId && (order.userId.firstName || order.userId.lastName)) { %>
                      <%= order.userId.firstName || '' %> <%= order.userId.lastName || '' %>
                    <% } else if (order.fullName) { %>
                      <%= order.fullName %>
                    <% } else { %>
                      Guest
                    <% } %>
                  </strong>
                </div>

                <% if (order.note && order.note.trim() !== '') { %>
                  <div class="mb-2">
                    <i class="bi bi-sticky me-2"></i>
                    <strong>Note:</strong> <%= order.note %>
                  </div>
                <% } %>
              </div>

              <div class="text-end">
                <span class="badge bg-warning d-block mb-2">Processing</span>
                <span class="time-badge">
                  <%= new Date(order.createdAt).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) %>
                </span>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>
</div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  // Auto-refresh every 3 seconds
  setTimeout(function() {
    window.location.reload();
  }, 3000);
</script>

</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Unauthorized Access</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
    .card {
      max-width: 420px;
      border-radius: 1rem;
      animation: pop 0.4s ease;
    }
    @keyframes pop {
      0% { transform: scale(0.9); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }
    .gif-container img {
      width: 100%;
      border-radius: 0.75rem;
    }
  </style>
</head>
<body>
  <div class="card shadow-lg p-4 text-center">
    <div class="gif-container mb-3">
      <img src="https://media.giphy.com/media/jUwpNzg9IcyrK/giphy.gif" alt="Huli ka Balbon GIF">
    </div>
    <h4 class="fw-bold text-danger mb-3">Huli Ka Balbon! 🚨</h4>
    <p class="text-muted mb-2">
      You cannot open this page directly: <br>
      <strong><%= requestedUrl %></strong>
    </p>
    <p class="small text-muted">Your IP: <%= clientIp %></p>
    <!-- Updated button to return to last view -->
    <a href="javascript:history.back()" class="btn btn-primary rounded-pill mt-3 px-4">Return to Last Page</a>
  </div>
</body>
</html>

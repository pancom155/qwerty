<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Unauthorized Access</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      height: 100vh;
      width: 100%;
      background-color: #000;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-family: "Poppins", sans-serif;
      text-align: center;
      overflow: hidden;
    }

    img {
      width: 360px;
      height: auto;
      margin-bottom: 2rem;
      animation: fadeIn 1.2s ease;
    }

    h1 {
      font-size: 2.2rem;
      font-weight: 700;
      color: #ff4d4f;
      margin-bottom: 0.75rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      text-shadow: 0 0 8px rgba(255, 77, 79, 0.8);
    }

    p {
      font-size: 1rem;
      color: #ccc;
      margin-bottom: 1rem;
    }

    .info {
      font-size: 0.9rem;
      color: #888;
      margin-bottom: 2rem;
      background: rgba(255, 255, 255, 0.05);
      padding: 10px 20px;
      border-radius: 10px;
    }

    .btn-custom {
      background: #0d6efd;
      border: none;
      color: #fff;
      font-weight: 600;
      padding: 0.6rem 1.8rem;
      border-radius: 50px;
      transition: all 0.3s ease;
    }

    .btn-custom:hover {
      background: #0b5ed7;
      transform: translateY(-2px);
      box-shadow: 0 0 20px rgba(13, 110, 253, 0.3);
    }

    footer {
      position: absolute;
      bottom: 15px;
      font-size: 0.8rem;
      color: #666;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <img src="https://www.lifewire.com/thmb/fvqMJHbQIwBUyM-gJnKKIUVDQ7c=/1500x0/filters:no_upscale():max_bytes(150000):strip_icc()/fix-magic-mouse-disconnect-problems-2260761-2b63cbb2800b44c2935c7e817846fd5a.gif" alt="Error 404 GIF">
  
  <h1>Unauthorized Access </h1>
  <p>You do not have permission to view this page directly.</p>
  
  <div class="info">
    <p>Requested URL: <strong><%= requestedUrl %></strong></p>
    <p>Your IP: <span><%= clientIp %></span></p>
  </div>

  <a href="javascript:history.back()" class="btn btn-custom">
    <i class="bi bi-arrow-left-circle me-1"></i> Return to Last Page
  </a>

  <footer>Access restricted © <script>document.write(new Date().getFullYear())</script></footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Order Success</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      background: #ffffff;
      color: #333;
      font-family: 'Poppins', sans-serif;
    }
    .success-container {
      text-align: center;
      padding-top: 30px;
      animation: fadeInUp 0.8s ease forwards;
    }
    .success-icon {
      font-size: 80px;
      color: #28a745;
      animation: bounce 1.5s infinite;
    }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-15px); }
    }
    .order-summary {
      max-width: 800px;
      margin: 20px auto;
    }
    .product-card {
      background: #f8f9fa;
      border: 1px solid #ddd;
      border-radius: 12px;
      display: flex;
      align-items: center;
      gap: 15px;
      margin-bottom: 15px;
      padding: 10px;
      color: #333;
    }
    .product-card img {
      width: 60px;
      height: 60px;
      object-fit: cover;
      border-radius: 8px;
    }
  </style>
</head>
<body>
  <div class="success-container">
    <div class="success-icon">
      <i class="bi bi-check-circle-fill"></i>
    </div>
    <h1 class="fw-bold mt-3">Order Placed Successfully!</h1>
    <p class="lead">Thank you for your purchase. You can now pick up your order.</p>

    <div class="order-summary">
      <h4 class="text-center mb-4">Your Order</h4>

      <% order.items.forEach(item => { %>
        <div class="product-card shadow-sm">
          <img src="/uploads/<%= item.image || (item.productId?.image || 'default.png') %>" alt="<%= item.name %>">
          <div class="flex-grow-1">
            <div class="fw-semibold"><%= item.name %></div>
            <small>₱<%= item.price.toFixed(2) %> × <%= item.quantity %></small>
          </div>
          <div class="fw-bold">
            ₱<%= item.subtotal.toFixed(2) %>
          </div>
        </div>
      <% }) %>

      <div class="d-flex justify-content-between mt-4 fw-bold fs-5">
        <span>Net Total:</span>
        <span>
          ₱<%= (order.items.reduce((sum, i) => sum + i.subtotal, 0) - (order.discounts?.reduce((sum, d) => sum + d.amount, 0) || 0)).toFixed(2) %>
        </span>
      </div>

      <div class="text-center mt-4 d-flex justify-content-center gap-2 flex-wrap">
        <a href="/user/orders" class="btn btn-dark">View My Orders</a>
        <a href="/user/menu" class="btn btn-outline-secondary">Go to Home</a>
      </div>
    </div>
  </div>
</body>
</html>
